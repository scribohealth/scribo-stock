# Cloudflare D1 (see wrangler.jsonc)
# 1. Create: wrangler d1 create scribo-stock-db
# 2. Put database_id in wrangler.jsonc under d1_databases[0].database_id
# 3. Apply migrations: wrangler d1 migrations apply scribo-stock-db --local
#    (use --remote for production)
#
# Optional: generate binding types
# bun run cf-typegen
